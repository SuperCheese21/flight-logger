{
  "name": "FlightLogger",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "npm run build && npm run start:server",
    "server": "node ./server/bin/www",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "update:airports": "node ./server/utils/updateAirports"
  }
}
